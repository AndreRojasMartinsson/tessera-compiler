export function w $main(w %argc.203, l %argv.204) {
@start
  # Load variable from stack
  %a =l loadl %argv.204
  # Get index 1 (long so it's 8 bytes (64 bit) )
  %b =l add %a, 8

	call $printf(l $main.205, w %argc.203)
	call $printf(l $main.206, l %argv.204)
	call $printf(l $fmt, l %b)
	ret 0
}

data $fmt = { b "Hi: %s\n", b 0 }
